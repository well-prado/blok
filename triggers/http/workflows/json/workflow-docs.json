{
	"name": "Workflow Docs Generator",
	"version": "1.0.0",
	"description": "This workflow generates documentation for a given workflow in HTML format.",
	"trigger": {
		"http": {
			"method": "ANY",
			"path": "/workflow-docs/:function?",
			"accept": "application/json"
		}
	},
	"steps": [
		{
			"id": "filter-request",
			"branch": {
				"when": "ctx.request.method.toLowerCase() === \"get\" && ctx.request.params.function === undefined",
				"then": [
					{
						"id": "workflow-ui",
						"use": "workflow-ui",
						"type": "module"
					}
				],
				"else": [
					{
						"id": "method-not-allowed",
						"use": "error",
						"type": "module"
					}
				]
			}
		}
	]
}
