apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "blok.configMapName" . }}
  labels:
    {{- include "blok.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
