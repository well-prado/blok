// Placeholder — `blokctl gen app-types --out src` overwrites this file with the
// real route table scanned from your Blok workflows. It is imported for its
// side effect so `route("name")` from `@blokjs/inertia-client` can build URLs.
import { registerRoutes } from "@blokjs/inertia-client";

registerRoutes({});
