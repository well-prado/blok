---
"@blokjs/agent-kernel": minor
---

Add provenance-aware context assembly with deterministic ordering,
freshness/invalidation handling, bounded budgets, and an observable
provider-neutral compaction seam.
