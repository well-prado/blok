// The CLI replaces this file in a project copy when Swift nodes are scaffolded.
// Keeping a no-op shim in the SDK makes a pristine checkout buildable.
public enum GeneratedUserNodeRegistry {
    public static func registerAll(_ registry: NodeRegistry) {}
}
