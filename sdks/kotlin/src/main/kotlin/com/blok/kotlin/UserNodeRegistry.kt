// Overwritten by blokctl codegen in a project (see generateKotlinNodeRegistry).
package com.blok.kotlin

/** The pristine SDK intentionally has no user nodes. */
object UserNodeRegistry {
    fun registerUserNodes(registry: NodeRegistry) = Unit
}
