// Superseded by the Radix-backed primitive (CONVENTIONS.md §6). Same exported
// name, same props, same on-screen text — call sites are migrated in a later PR.
export { ExportMenu } from "@/components/primitives/DropdownMenu";
