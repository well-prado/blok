// Superseded by the design-system primitive (CONVENTIONS §6). Kept as a shim so
// the eleven existing call sites stay untouched; a later PR migrates the imports
// and deletes this file.
export { EmptyState } from "@/components/primitives/EmptyState";
