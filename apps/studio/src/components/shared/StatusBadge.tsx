// Superseded by the design-system primitive (CONVENTIONS §6). Kept as a shim so
// the nine existing call sites and the frozen component test stay untouched; a
// later PR migrates the imports and deletes this file.
export { StatusBadge } from "@/components/primitives/Badge";
