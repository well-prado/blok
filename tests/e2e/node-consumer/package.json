{
	"name": "blok-node-consumer-fixture",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "Minimal downstream consumer used by scripts/check-packed-exports.ts (#687). npm-installs the PACKED @blokjs tarballs and runs vitest on Node with zero resolution workarounds.",
	"scripts": {
		"test": "vitest run"
	}
}
