# Pin the workspace layout: bun ≥1.3.14 defaults to the "isolated" linker,
# which stops hoisting workspace packages to the root node_modules — every
# undeclared-but-relied-on import (scaffold templates, tests importing
# materialized templates) then fails to resolve. The repo assumes hoisting.
[install]
linker = "hoisted"
